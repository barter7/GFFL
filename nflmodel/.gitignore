# Regenerable data caches (rebuild with refresh_cache() / run_model.R)
data/cache/
data/props/
data/pbp/
data/context/game_context.csv
data/results/report_*.html
# data/context/coordinators.csv IS committed once generated
# (scrape_coordinators.R) — it is hand-reviewed, not regenerable
